[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "env CHECKPOINT_DISABLE=1 npx prisma db push --skip-generate; echo '=== Prisma done, PORT='$PORT' ==='; npm run start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
